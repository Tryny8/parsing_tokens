[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "parsing-tokens"
version = "0.1.0"
description = "Text tokenization and parsing library (WIP)"
authors = [{name = "Ton Nom"}]
readme = "README.md"
requires-python = ">=3.10"